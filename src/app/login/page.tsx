'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function Login(){const r=useRouter(),[err,setErr]=useState('');async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const d=await res.json();if(!res.ok)return setErr(d.error);r.push(d.role==='CUSTOMER'?'/customer':d.role==='TRANSPORTER'?'/transporter':'/admin')}return <main className="shell"><div className="card" style={{maxWidth:520,margin:'auto'}}><h1>Welcome back</h1><form onSubmit={submit}><div className="field"><label>EMAIL</label><input type="email" name="email" required/></div><div className="field"><label>PASSWORD</label><input type="password" name="password" required/></div>{err&&<p className="error">{err}</p>}<button className="btn orange">Sign in</button></form></div></main>}
