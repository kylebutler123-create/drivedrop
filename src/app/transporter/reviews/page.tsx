'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
type Review={id:string;rating:number;body:string|null;verified:boolean;createdAt:string;customerName:string;vehicleMake:string;vehicleModel:string;transporterResponse:string|null;transporterRespondedAt:string|null;moderationStatus:string;disputeReason:string|null;disputeDetails:string|null;moderationNote:string|null;moderatedAt:string|null};
const stars=(n:number)=>'★'.repeat(n)+'☆'.repeat(5-n);const moderationLabel=(r:Review)=>r.moderationStatus==='UNDER_REVIEW'?'Under DriveDrop review':r.moderationStatus==='HIDDEN'?'Hidden by DriveDrop':r.moderationStatus==='DISPUTE_REJECTED'?'Dispute closed':r.verified?'Verified review':'Review';
export default function TransporterReviews(){const[reviews,setReviews]=useState<Review[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState(''),[responding,setResponding]=useState<string|null>(null),[response,setResponse]=useState(''),[disputing,setDisputing]=useState<string|null>(null),[reason,setReason]=useState(''),[details,setDetails]=useState(''),[expanded,setExpanded]=useState<string|null>(null);async function load(){const r=await fetch('/api/transporter/reviews',{cache:'no-store'});if(r.ok)setReviews(await r.json());setLoading(false)}useEffect(()=>{load()},[]);async function saveResponse(reviewId:string){setMessage('');const r=await fetch('/api/transporter/reviews',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action:'RESPOND',reviewId,response})});const d=await r.json().catch(()=>({}));if(!r.ok)return setMessage(d.error||'Unable to add response');setMessage('Your public response has been added.');setResponding(null);setResponse('');await load()}async function dispute(reviewId:string){setMessage('');const r=await fetch('/api/transporter/reviews',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({action:'DISPUTE',reviewId,reason,details:details||undefined})});const d=await r.json().catch(()=>({}));if(!r.ok)return setMessage(d.error||'Unable to dispute review');setMessage('Review dispute submitted to DriveDrop.');setDisputing(null);setReason('');setDetails('');await load()}const visible=reviews.filter(r=>r.moderationStatus!=='HIDDEN');const average=visible.length?visible.reduce((n,r)=>n+r.rating,0)/visible.length:0;return <main className="shell dashboardShell transporterReviewsPage"><Link className="backLink" href="/transporter">← Back to transporter dashboard</Link><header className="dashboardHero"><div><span className="dashboardEyebrow">Customer feedback</span><h1>Your customer reviews</h1><p>View verified customer feedback, publish a response, or ask DriveDrop to review content you believe is unfair or inappropriate.</p></div><div className="adminHeroBadge transporterRatingBadge"><span>Current rating</span><strong>{visible.length?average.toFixed(1):'—'}</strong><small>{visible.length} visible review{visible.length===1?'':'s'}</small></div></header>{message&&<div className="formNotice successNotice">{message}</div>}{loading?<div className="dashboardCard">Loading reviews…</div>:reviews.length===0?<div className="dashboardCard emptyState"><div>★</div><h3>No customer reviews yet</h3><p>Verified reviews will appear here after customers complete deliveries.</p></div>:<div className="transporterReviewList">{reviews.map(r=>{const open=expanded===r.id;return <article className={`dashboardCard transporterReviewCard ${open?'isExpanded':''}`} key={r.id}><button type="button" className="transporterReviewSummary" onClick={()=>setExpanded(open?null:r.id)} aria-expanded={open}><div className="transporterReviewIdentity"><span className="statusPill">{moderationLabel(r)}</span><strong>{r.customerName}</strong><small>{r.vehicleMake} {r.vehicleModel}</small></div><div className="transporterReviewQuick"><span><small>Rating</small><b className="reviewStars">{stars(r.rating)} <em>{r.rating}/5</em></b></span><span><small>Submitted</small><b>{new Date(r.createdAt).toLocaleDateString('en-GB')}</b></span><span><small>Response</small><b>{r.transporterResponse?'Published':'Not added'}</b></span></div><span className="transporterReviewChevron">{open?'−':'+'}</span></button>{open&&<div className="transporterReviewExpanded"><div className="reviewContext"><span><small>Customer</small><b>{r.customerName}</b></span><span><small>Vehicle</small><b>{r.vehicleMake} {r.vehicleModel}</b></span><span><small>Rating</small><b>{stars(r.rating)} · {r.rating}/5</b></span><span><small>Review date</small><b>{new Date(r.createdAt).toLocaleDateString('en-GB')}</b></span></div><div className="infoPanel reviewTextPanel"><b>Customer review</b><p>{r.body||'No written comment was provided.'}</p></div>{r.transporterResponse&&<div className="infoPanel reviewTextPanel responsePanel"><b>Your public response</b><p>{r.transporterResponse}</p>{r.transporterRespondedAt&&<small>Published {new Date(r.transporterRespondedAt).toLocaleDateString('en-GB')}</small>}</div>}{r.moderationStatus==='UNDER_REVIEW'&&<div className="formNotice"><strong>Review dispute under review</strong><div>Reason: {r.disputeReason}</div>{r.disputeDetails&&<div>{r.disputeDetails}</div>}</div>}{r.moderationStatus!=='UNDER_REVIEW'&&r.moderationNote&&<div className="formNotice"><strong>DriveDrop moderation outcome</strong><div>{r.moderationNote}</div>{r.moderatedAt&&<small>Reviewed {new Date(r.moderatedAt).toLocaleDateString('en-GB')}</small>}</div>}{r.moderationStatus!=='HIDDEN'&&(responding===r.id?<div className="infoPanel reviewActionPanel"><div className="field"><label>PUBLIC RESPONSE</label><textarea rows={4} maxLength={1200} value={response} onChange={e=>setResponse(e.target.value)} placeholder="Write a professional response that customers can see."/></div><div className="actionButtons"><button className="btn orange" disabled={!response.trim()} onClick={()=>saveResponse(r.id)}>Publish response</button><button className="btn light" onClick={()=>{setResponding(null);setResponse('')}}>Cancel</button></div></div>:<button className="btn light reviewActionButton" onClick={()=>{setResponding(r.id);setResponse(r.transporterResponse||'')}}>{r.transporterResponse?'Edit public response':'Add public response'}</button>)}{r.moderationStatus!=='UNDER_REVIEW'&&r.moderationStatus!=='HIDDEN'&&(disputing===r.id?<div className="infoPanel reviewActionPanel"><h3>Dispute this review</h3><p className="muted">Explain why DriveDrop should review this customer feedback. The review stays visible while moderation is in progress.</p><div className="field"><label>REASON</label><input maxLength={160} value={reason} onChange={e=>setReason(e.target.value)} placeholder="e.g. Contains inaccurate or abusive content"/></div><div className="field"><label>DETAILS</label><textarea rows={4} maxLength={2000} value={details} onChange={e=>setDetails(e.target.value)} placeholder="Provide any context that will help DriveDrop review the case."/></div><div className="actionButtons"><button className="btn orange" disabled={reason.trim().length<3} onClick={()=>dispute(r.id)}>Submit to DriveDrop</button><button className="btn light" onClick={()=>{setDisputing(null);setReason('');setDetails('')}}>Cancel</button></div></div>:<button className="btn light reviewActionButton secondary" onClick={()=>setDisputing(r.id)}>Dispute review</button>)}</div>}</article>})}</div>}</main>}
