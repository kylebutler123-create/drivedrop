import {NextResponse} from 'next/server';import {currentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';import {randomUUID,createCipheriv,createHash,randomBytes} from 'crypto';import {z} from 'zod';
const S=z.object({accountHolderName:z.string().trim().min(2).max(120),sortCode:z.string().transform(v=>v.replace(/\D/g,'')).pipe(z.string().length(6)),accountNumber:z.string().transform(v=>v.replace(/\D/g,'')).pipe(z.string().min(6).max(8))});
function key(){const secret=process.env.AUTH_SECRET||process.env.NEXTAUTH_SECRET||'';if(!secret)throw new Error('Server encryption key is not configured');return createHash('sha256').update(`drivedrop:payout:${secret}`).digest()}
function seal(value:string){const iv=randomBytes(12),cipher=createCipheriv('aes-256-gcm',key(),iv);const enc=Buffer.concat([cipher.update(value,'utf8'),cipher.final()]),tag=cipher.getAuthTag();return Buffer.concat([iv,tag,enc]).toString('base64url')}
export async function GET(){const u=await currentUser();if(!u||u.role!=='TRANSPORTER')return NextResponse.json({error:'Transporter access required'},{status:403});const rows=await prisma.$queryRaw<any[]>`SELECT "accountHolderName","sortCodeLast2","accountNumberLast4","completedAt","updatedAt" FROM "TransporterPayoutDetails" WHERE "userId"=${u.id} LIMIT 1`;const d=rows[0];return NextResponse.json(d?{complete:true,accountHolderName:d.accountHolderName,sortCodeMasked:`••-••-${d.sortCodeLast2}`,accountNumberMasked:`••••${d.accountNumberLast4}`,completedAt:d.completedAt,updatedAt:d.updatedAt}:{complete:false})}
export async function POST(r:Request){const u=await currentUser();if(!u||u.role!=='TRANSPORTER')return NextResponse.json({error:'Transporter access required'},{status:403});const parsed=S.safeParse(await r.json());if(!parsed.success)return NextResponse.json({error:'Enter a valid account holder name, 6-digit sort code and account number'},{status:400});const d=parsed.data,token=seal(JSON.stringify({sortCode:d.sortCode,accountNumber:d.accountNumber})),now=new Date();await prisma.$executeRaw`INSERT INTO "TransporterPayoutDetails" ("id","userId","accountHolderName","sortCodeLast2","accountNumberLast4","detailsToken","completedAt","updatedAt") VALUES (${randomUUID()},${u.id},${d.accountHolderName},${d.sortCode.slice(-2)},${d.accountNumber.slice(-4)},${token},${now},${now}) ON CONFLICT ("userId") DO UPDATE SET "accountHolderName"=EXCLUDED."accountHolderName","sortCodeLast2"=EXCLUDED."sortCodeLast2","accountNumberLast4"=EXCLUDED."accountNumberLast4","detailsToken"=EXCLUDED."detailsToken","updatedAt"=EXCLUDED."updatedAt"`;return NextResponse.json({ok:true,complete:true,accountHolderName:d.accountHolderName,sortCodeMasked:`••-••-${d.sortCode.slice(-2)}`,accountNumberMasked:`••••${d.accountNumber.slice(-4)}`})}
