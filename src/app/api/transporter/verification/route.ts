import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {currentUser} from '@/lib/auth';import {z} from 'zod';
const S=z.object({businessName:z.string().min(2),companyNumber:z.string().max(30).optional(),businessAddress:z.string().min(5),phone:z.string().min(7).max(30),yearsOperating:z.number().int().min(0).max(150).optional(),website:z.string().url().optional().or(z.literal(''))});
export async function GET(){const u=await currentUser();if(!u||u.role!=='TRANSPORTER')return NextResponse.json({error:'Transporter access required'},{status:403});return NextResponse.json(await prisma.transporterVerification.findUnique({where:{transporterId:u.id},include:{documents:{orderBy:{createdAt:'desc'}}}}))}
export async function PUT(r:Request){const u=await currentUser();if(!u||u.role!=='TRANSPORTER')return NextResponse.json({error:'Transporter access required'},{status:403});const x=S.safeParse(await r.json());if(!x.success)return NextResponse.json({error:'Invalid business details'},{status:400});const data={...x.data,website:x.data.website||null};const v=await prisma.transporterVerification.upsert({where:{transporterId:u.id},create:{...data,transporterId:u.id},update:data,include:{documents:true}});return NextResponse.json(v)}
export async function POST(){const u=await currentUser();if(!u||u.role!=='TRANSPORTER')return NextResponse.json({error:'Transporter access required'},{status:403});const v=await prisma.transporterVerification.findUnique({where:{transporterId:u.id},include:{documents:true}});if(!v)return NextResponse.json({error:'Complete your business details first'},{status:400});if(!v.documents.length)return NextResponse.json({error:'Upload at least one verification document first'},{status:400});return NextResponse.json(await prisma.transporterVerification.update({where:{id:v.id},data:{status:'PENDING',submittedAt:new Date(),reviewNote:null}}))}
