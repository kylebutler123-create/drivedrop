import { NextResponse } from 'next/server';import { prisma } from '@/lib/prisma';import { currentUser } from '@/lib/auth';export async function GET(){const u=await currentUser();if(!u||u.role!=='CUSTOMER')return NextResponse.json({error:'Forbidden'},{status:403});return NextResponse.json(await prisma.transportJob.findMany({where:{customerId:u.id},include:{quotes:{include:{transporter:{select:{name:true,transporterVerification:{select:{status:true}}}}}},booking:true},orderBy:{createdAt:'desc'}}))}
