import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {currentUser} from '@/lib/auth';
export async function GET(){const u=await currentUser();if(!u)return NextResponse.json({error:'Unauthorized'},{status:401});const where=u.role==='CUSTOMER'?{customerId:u.id}:u.role==='TRANSPORTER'?{transporterId:u.id}:{};return NextResponse.json(await prisma.booking.findMany({where,include:{job:true,customer:{select:{name:true}},transporter:{select:{name:true}},trackingEvents:{orderBy:{createdAt:'asc'}},messages:{include:{sender:{select:{name:true,role:true}}},orderBy:{createdAt:'asc'}},evidence:{orderBy:{createdAt:'asc'}},review:true,payment:{include:{events:{orderBy:{createdAt:'asc'}}}}},orderBy:{createdAt:'desc'}}))}
