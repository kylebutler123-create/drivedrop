import {NextResponse} from 'next/server';import {prisma} from '@/lib/prisma';import {currentUser} from '@/lib/auth';import {z} from 'zod';
const S=z.object({verificationId:z.string(),status:z.enum(['APPROVED','REJECTED','SUSPENDED']),reviewNote:z.string().max(1000).optional()});
export async function GET(){const u=await currentUser();if(!u||u.role!=='ADMIN')return NextResponse.json({error:'Admin access required'},{status:403});return NextResponse.json(await prisma.transporterVerification.findMany({include:{transporter:{select:{name:true,email:true,accountStatus:true,workRestricted:true}},documents:{orderBy:{createdAt:'asc'}}},orderBy:[{status:'asc'},{submittedAt:'desc'}]}))}
export async function PATCH(r:Request){const u=await currentUser();if(!u||u.role!=='ADMIN')return NextResponse.json({error:'Admin access required'},{status:403});const x=S.safeParse(await r.json());if(!x.success)return NextResponse.json({error:'Invalid review'},{status:400});return NextResponse.json(await prisma.transporterVerification.update({where:{id:x.data.verificationId},data:{status:x.data.status,reviewNote:x.data.reviewNote,reviewedAt:new Date(),reviewerId:u.id}}))}
