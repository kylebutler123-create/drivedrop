'use client';
import {FormEvent,useEffect,useMemo,useRef,useState} from 'react';
import Link from 'next/link';

type Attachment={id:string;mimeType:string;createdAt?:string};
type Booking={id:string;status:string;customer:{name:string};transporter:{name:string};job:{vehicleMake:string;vehicleModel:string;collection:string;delivery:string};messages:{id:string;body:string;createdAt:string;sender:{name:string;role:string}}[]};
type Me={id:string;name:string;role:'CUSTOMER'|'TRANSPORTER'|'ADMIN'};
type Message={id:string;body:string;createdAt:string;readAt?:string|null;senderId?:string;sender:{name:string;role:string};attachments?:Attachment[]};
const label=(s:string)=>s.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());

export default function MessagesPage(){
 const[me,setMe]=useState<Me|null>(null),[bookings,setBookings]=useState<Booking[]>([]),[selectedId,setSelectedId]=useState<string|null>(null),[messages,setMessages]=useState<Message[]>([]),[unreadByBooking,setUnreadByBooking]=useState<Record<string,number>>({});
 const[loading,setLoading]=useState(true),[sending,setSending]=useState(false),[body,setBody]=useState(''),[notice,setNotice]=useState(''),[images,setImages]=useState<File[]>([]);const fileRef=useRef<HTMLInputElement>(null);
 async function loadBookings(){const[m,b]=await Promise.all([fetch('/api/me',{cache:'no-store'}),fetch('/api/my-bookings',{cache:'no-store'})]);if(m.ok)setMe(await m.json());if(b.ok){const rows=await b.json();setBookings(rows);setSelectedId(x=>x&&rows.some((r:Booking)=>r.id===x)?x:null)}setLoading(false)}
 async function loadUnread(){const r=await fetch('/api/messages/unread-count',{cache:'no-store'});if(r.ok){const d=await r.json();setUnreadByBooking(d.byBooking||{})}}
 async function markRead(bookingId:string){const r=await fetch('/api/bookings/messages',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({bookingId})});if(r.ok){setUnreadByBooking(x=>({...x,[bookingId]:0}));window.dispatchEvent(new Event('drivedrop:messages-read'))}}
 async function loadMessages(bookingId:string){const r=await fetch(`/api/bookings/messages?bookingId=${encodeURIComponent(bookingId)}`,{cache:'no-store'});if(r.ok){setMessages(await r.json());await markRead(bookingId)}}
 function selectBooking(id:string){setSelectedId(id);setNotice('');setImages([]);setTimeout(()=>document.getElementById('selected-conversation')?.scrollIntoView({behavior:'smooth',block:'start'}),50)}
 useEffect(()=>{loadBookings();loadUnread();const t=setInterval(()=>{loadBookings();loadUnread()},5000);return()=>clearInterval(t)},[]);useEffect(()=>{if(!selectedId){setMessages([]);return}loadMessages(selectedId);const t=setInterval(()=>loadMessages(selectedId),3000);return()=>clearInterval(t)},[selectedId]);
 const selected=useMemo(()=>bookings.find(b=>b.id===selectedId)||null,[bookings,selectedId]);
 async function send(e:FormEvent){e.preventDefault();if(!selectedId||(!body.trim()&&!images.length))return;setSending(true);setNotice('');try{const f=new FormData();f.set('bookingId',selectedId);f.set('body',body);images.forEach(img=>f.append('images',img));const r=await fetch('/api/bookings/messages',{method:'POST',body:f});const d=await r.json().catch(()=>({}));if(!r.ok){setNotice(d.error||'Unable to send message');return}setBody('');setImages([]);if(fileRef.current)fileRef.current.value='';setMessages(x=>[...x,d]);await loadMessages(selectedId)}finally{setSending(false)}}
 if(loading)return <main className="shell dashboardShell"><div className="dashboardCard">Loading conversations…</div></main>;
 if(!me)return <main className="shell dashboardShell"><div className="dashboardCard"><h1>Messages</h1><p>Please sign in to view booking conversations.</p><Link className="btn orange" href="/login">Sign in</Link></div></main>;
 const dashboard=me.role==='CUSTOMER'?'/customer':me.role==='TRANSPORTER'?'/transporter':'/admin';
 const unreadTotal=Object.values(unreadByBooking).reduce((n,v)=>n+v,0);
 return <main className="shell dashboardShell messagesShell"><Link className="backLink" href={dashboard}>← Back to dashboard</Link><header className="dashboardHero messagesHero"><div><span className="dashboardEyebrow">Booking conversations</span><h1>Your messages</h1><p>Keep customer and transporter communication together with the relevant vehicle delivery.</p></div><div className="messagesHeroStats"><div><span>Conversations</span><strong>{bookings.length}</strong></div><div><span>Unread</span><strong>{unreadTotal}</strong></div></div></header>{bookings.length===0?<div className="dashboardCard emptyState"><div>💬</div><h3>No booking conversations yet</h3><p>Messages become available once a quote has been accepted and a booking is created.</p></div>:<div className="messagesLayout"><section className="dashboardCard conversationListPanel"><div className="conversationListHeading"><div><span className="dashboardEyebrow dark">Inbox</span><h2>Conversations</h2></div><span>{bookings.length}</span></div><div className="conversationList">{bookings.map(b=>{const other=me.role==='CUSTOMER'?b.transporter.name:b.customer.name;const latest=b.messages?.[b.messages.length-1];const active=b.id===selectedId;const unread=unreadByBooking[b.id]||0;return <button type="button" key={b.id} onClick={()=>selectBooking(b.id)} aria-pressed={active} className={`conversationCard ${active?'isActive':''} ${unread?'hasUnread':''}`}><div className="conversationTop"><div><span className="conversationVehicle">{b.job.vehicleMake} {b.job.vehicleModel}</span><small>{other}</small></div><div className="conversationMeta">{unread>0&&<span className="conversationUnread">{unread}</span>}<span>{label(b.status)}</span></div></div><p>{latest?(latest.body==='Photo'?'📷 Picture message':latest.body):'No messages yet'}</p>{latest&&<small className="conversationTime">{new Date(latest.createdAt).toLocaleString('en-GB',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</small>}</button>})}</div></section><section id="selected-conversation" className="dashboardCard conversationPanel">{selected?<><div className="conversationHeader"><div><span className="statusPill">{label(selected.status)}</span><h2>{selected.job.vehicleMake} {selected.job.vehicleModel}</h2><p>{me.role==='CUSTOMER'?'Transporter':'Customer'} · <b>{me.role==='CUSTOMER'?selected.transporter.name:selected.customer.name}</b></p></div><div className="conversationContext"><span>{selected.job.collection}</span><b>→</b><span>{selected.job.delivery}</span></div></div><div className="messageStream">{messages.length===0?<div className="emptyState messageEmpty"><div>💬</div><h3>Start the conversation</h3><p>Send a message or picture about this delivery.</p></div>:messages.map(m=>{const mine=m.senderId===me.id||m.sender.name===me.name;return <div key={m.id} className={`messageRow ${mine?'mine':'theirs'}`}><div className="messageBubble messageBubblePolished"><div className="messageSender">{mine?'You':m.sender.name}</div>{m.body&&m.body!=='Photo'&&<span className="messageBody">{m.body}</span>}{m.attachments&&m.attachments.length>0&&<div className={`messageAttachmentGrid count${Math.min(m.attachments.length,4)}`}>{m.attachments.map(a=><a key={a.id} href={`/api/messages/attachments/${a.id}`} target="_blank" rel="noreferrer" className="messageAttachment"><img src={`/api/messages/attachments/${a.id}`} alt="Message attachment"/></a>)}</div>}<small className="messageTimestamp">{new Date(m.createdAt).toLocaleString('en-GB')}</small></div></div>})}</div><form onSubmit={send} className="messageComposer"><div className="messageComposerTop"><div className="field messageTextField"><label>MESSAGE</label><textarea rows={3} maxLength={2000} value={body} onChange={e=>setBody(e.target.value)} placeholder="Write a message about this booking…"/></div><div className="messagePictureControl"><label className="messageAttachButton">📷 Add pictures<input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp" multiple onChange={e=>{const files=Array.from(e.target.files||[]).slice(0,4);setImages(files);if((e.target.files?.length||0)>4)setNotice('You can attach up to 4 pictures per message.')}}/></label><small>JPG, PNG or WebP · up to 4</small></div></div>{images.length>0&&<div className="selectedAttachments">{images.map((img,i)=><span key={`${img.name}-${i}`}>📷 {img.name.length>24?`${img.name.slice(0,21)}…`:img.name}</span>)}<button type="button" className="textAction" onClick={()=>{setImages([]);if(fileRef.current)fileRef.current.value=''}}>Remove pictures</button></div>}{notice&&<div className="formNotice errorNotice">{notice}</div>}<div className="messageComposerActions"><button className="btn orange" disabled={sending||(!body.trim()&&!images.length)}>{sending?'Sending…':images.length?`Send ${images.length>1?'message & pictures':'message & picture'}`:'Send message'}</button></div></form></>:<div className="emptyState messageSelectEmpty"><div>💬</div><h3>Select a conversation</h3><p>Choose a booking to read and reply to messages.</p></div>}</section></div>}</main>;
}
