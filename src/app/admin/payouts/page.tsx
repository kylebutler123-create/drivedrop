import Link from 'next/link';import {currentUser} from '@/lib/auth';import {prisma} from '@/lib/prisma';import PayoutDashboard from './PayoutDashboard';
const money=(p:number)=>new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP'}).format(p/100);export const dynamic='force-dynamic';export const revalidate=0;
export default async function AdminPayouts({searchParams}:{searchParams:Promise<{action?:string}>}){const params=await searchParams;const action=params.action==='ready'||params.action==='blocked'?params.action:null;const u=await currentUser();if(!u||u.role!=='ADMIN')return <main className="shell dashboardShell"><div className="dashboardCard adminAccess"><div className="adminLock">🔐</div><span className="dashboardEyebrow dark">Restricted area</span><h1>Admin access</h1><p>Administrator authentication is required to manage payouts.</p><Link className="btn orange" href="/login">Sign in as administrator</Link></div></main>;const payments:any[]=await prisma.bookingPayment.findMany({where:{payoutStatus:{in:['READY','HELD','PAID']}},include:{booking:{include:{job:true,customer:{select:{name:true,email:true}},transporter:{select:{id:true,name:true,email:true}},disputes:{orderBy:{createdAt:'desc'}},trackingEvents:{where:{status:'DELIVERED'},orderBy:{createdAt:'desc'},take:1},evidence:{orderBy:{createdAt:'asc'}}}},events:{where:{type:{in:['PAYOUT_READY','PAYOUT_PAID']}},orderBy:{createdAt:'desc'}}},orderBy:{updatedAt:'desc'}});const ids=[...new Set(payments.map(p=>p.booking.transporter.id))];const jobIds=[...new Set(payments.map(p=>p.booking.job.id))];const [rows,vehicleRows]=await Promise.all([ids.length?prisma.$queryRawUnsafe<any[]>(`SELECT "userId" FROM "TransporterPayoutDetails" WHERE "userId" IN (${ids.map((_,i)=>`$${i+1}`).join(',')})`,...ids):Promise.resolve([]),jobIds.length?prisma.$queryRawUnsafe<Array<{id:string;vehicleType:string|null}>>(`SELECT "id", "vehicleType" FROM "TransportJob" WHERE "id" IN (${jobIds.map((_,i)=>`$${i+1}`).join(',')})`,...jobIds):Promise.resolve([])]);const complete=new Set(rows.map(r=>r.userId));const vehicleTypes=new Map(vehicleRows.map(r=>[r.id,r.vehicleType]));const prepared=payments.map(p=>({...p,booking:{...p.booking,job:{...p.booking.job,vehicleType:vehicleTypes.get(p.booking.job.id)||null}},payoutDetailsComplete:complete.has(p.booking.transporter.id)}));const ready=prepared.filter(p=>p.payoutStatus==='READY');const value=(a:any[])=>a.reduce((n,p)=>n+p.transporterProceedsPence,0);return <main className="shell dashboardShell adminShell"><Link className="backLink" href="/admin">← Back to Control Centre</Link><header className="dashboardHero adminHero"><div><span className="dashboardEyebrow">Finance operations</span><h1>Transporter payouts</h1><p>Review customer-confirmed payouts, protected holds and completed payout records.</p></div><div className="adminHeroBadge"><span>Ready to release</span><strong>{money(value(ready))}</strong><small>{ready.length} payout{ready.length===1?'':'s'}</small></div></header><PayoutDashboard rows={prepared} initialBlockedOnly={action==='blocked'}/></main>}