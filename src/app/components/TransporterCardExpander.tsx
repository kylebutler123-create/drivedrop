'use client';
import {useEffect} from 'react';
import {usePathname} from 'next/navigation';
function text(el:Element|null){return (el?.textContent||'').replace(/\s+/g,' ').trim()}
function escapeHtml(value:any){return String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]||c))}
const vehicleTypes=['Car','Motorcycle','Van','Motor home','Truck','Caravan','Plant machine','Farm machine'];
function detailValue(card:HTMLElement,label:string){const rows=Array.from(card.querySelectorAll<HTMLElement>('.activeVehicleDetailsGrid>span'));const row=rows.find(x=>text(x.querySelector('small')).toLowerCase()===label.toLowerCase());return text(row?.querySelector('b')||null)}
function deliveryFacts(card:HTMLElement){const vehicleType=detailValue(card,'Vehicle type')||'Not specified';const registration=detailValue(card,'Registration')||'Not provided';const running=detailValue(card,'Running condition')||'Not specified';const date=text(card.querySelector('.agreedCollectionDate strong'))||detailValue(card,'Collection date')||'Not specified';const proceeds=text(card.querySelector('.paymentMini strong'))||'—';return `<span class="transporterCollapsedFacts transporterDeliveryFacts"><span><small>Vehicle</small><b>${escapeHtml(vehicleType)}</b></span><span><small>Reg</small><b>${escapeHtml(registration)}</b></span><span><small>Date</small><b>${escapeHtml(date)}</b></span><span><small>Condition</small><b>${escapeHtml(running)}</b></span><span class="wide"><small>Proceeds</small><b>${escapeHtml(proceeds)}</b></span></span>`}
function refreshDeliveryFacts(card:HTMLElement){const main=card.querySelector<HTMLElement>('.transporterCardSummaryMain');if(!main)return;main.querySelector('.transporterDeliveryFacts')?.remove();main.insertAdjacentHTML('beforeend',deliveryFacts(card))}
function enhance(card:HTMLElement,job?:any){if(card.dataset.transporterExpandable==='true')return;card.dataset.transporterExpandable='true';card.classList.add('transporterExpandableCard','isCollapsed');if(job?.vehicleType)card.dataset.vehicleType=String(job.vehicleType);if(job?.id)card.dataset.jobId=String(job.id);const btn=document.createElement('button');btn.type='button';btn.className='transporterCardToggle';btn.setAttribute('aria-expanded','false');const status=text(card.querySelector('.statusPill'));const title=text(card.querySelector('.bookingTop h2'))||'DriveDrop card';const partner=text(card.querySelector('.bookingPartner'));const route=card.querySelector('.routeVisual');const routeStops=route?Array.from(route.querySelectorAll('b')).map(x=>text(x)).filter(Boolean):[];const proceeds=text(card.querySelector('.paymentMini strong'));const vehicleState=text(card.querySelector('.vehicleState b'));const kind=card.classList.contains('jobOpportunity')?'job':'delivery';const routeSummary=routeStops.length>=2?`<span class="transporterRouteSummary"><span><small>Collection</small><b>${escapeHtml(routeStops[0])}</b></span><i>→</i><span><small>Delivery</small><b>${escapeHtml(routeStops[1])}</b></span></span>`:`<span class="transporterRouteSummary"><span><small>Route</small><b>${escapeHtml(partner||'View route')}</b></span></span>`;let stat=kind==='job'?(vehicleState||'Open for quotes'):(proceeds?`Proceeds · ${proceeds}`:partner||'View delivery');let quickFacts=kind==='delivery'?deliveryFacts(card):'';if(kind==='job'&&job){const quoted=(job.quotes||[]).length>0;stat=quoted?'Already quoted':'Not yet quoted';quickFacts=`<span class="transporterCollapsedFacts"><span><small>Vehicle</small><b>${escapeHtml(job.vehicleType||'Not specified')}</b></span><span><small>Reg</small><b>${escapeHtml(job.registration||'Not provided')}</b></span><span><small>Date</small><b>${job.collectionDate?new Date(job.collectionDate).toLocaleDateString('en-GB',{day:'numeric',month:'short'}):'Not provided'}</b></span><span><small>Condition</small><b>${job.running?'Runs & drives':'Non-running'}</b></span></span>`}btn.innerHTML=`<span class="transporterCardSummaryMain"><span class="transporterCardSummaryStatus">${escapeHtml(status||(kind==='job'?'Open for quotes':'Delivery'))}</span><strong>${escapeHtml(title)}</strong>${routeSummary}${quickFacts}</span><span class="transporterCardSummarySide"><b>${escapeHtml(stat)}</b><span class="transporterCardChevron">+</span></span>`;const toggle=()=>{const collapsed=card.classList.toggle('isCollapsed');btn.setAttribute('aria-expanded',collapsed?'false':'true');const c=btn.querySelector('.transporterCardChevron');if(c)c.textContent=collapsed?'+':'−'};btn.addEventListener('click',toggle);if(kind==='delivery')card.addEventListener('drivedrop:agreed-date-ready',()=>refreshDeliveryFacts(card));card.insertBefore(btn,card.firstChild)}
function ensureJobFilters(jobs:any[]){const section=document.getElementById('available-jobs');const heading=section?.querySelector('.dashboardSectionHeading');if(!section||!heading)return;let bar=section.querySelector<HTMLElement>('[data-job-filters]');if(!bar){bar=document.createElement('div');bar.dataset.jobFilters='true';bar.style.cssText='display:flex;align-items:flex-end;gap:10px;flex-wrap:wrap;margin:0 0 14px;padding:12px 14px;border:1px solid #dfe7ef;border-radius:14px;background:#f8fafc';bar.innerHTML=`<label style="display:grid;gap:5px;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.04em;color:#52667e">Vehicle type<select data-vehicle-select aria-label="Filter available jobs by vehicle type" style="min-height:40px;padding:8px 34px 8px 11px;border:1px solid #cfd9e3;border-radius:10px;background:#fff;color:#183654;font-weight:700">${['ALL',...vehicleTypes].map(type=>`<option value="${escapeHtml(type)}">${type==='ALL'?'All vehicle types':escapeHtml(type)}</option>`).join('')}</select></label><label style="display:grid;gap:5px;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.04em;color:#52667e">Your postcode<input data-postcode-input placeholder="e.g. B1 1AA" maxlength="16" style="min-height:40px;box-sizing:border-box;padding:8px 11px;border:1px solid #cfd9e3;border-radius:10px;background:#fff;color:#183654;font-weight:700;width:140px"/></label><label style="display:grid;gap:5px;font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.04em;color:#52667e">Radius<select data-radius-select style="min-height:40px;padding:8px 34px 8px 11px;border:1px solid #cfd9e3;border-radius:10px;background:#fff;color:#183654;font-weight:700"><option value="10">10 miles</option><option value="25" selected>25 miles</option><option value="50">50 miles</option><option value="75">75 miles</option><option value="100">100 miles</option><option value="150">150 miles</option><option value="200">200 miles</option></select></label><button type="button" data-apply-distance class="btn orange" style="min-height:40px">Apply distance</button><button type="button" data-clear-distance class="btn light" style="min-height:40px;display:none">Clear distance</button><span data-filter-result style="margin-left:auto;color:#6c7d90;font-size:11px;font-weight:800"></span><span data-distance-message style="flex-basis:100%;color:#6c7d90;font-size:11px"></span>`;heading.insertAdjacentElement('afterend',bar)}
 const vehicle=bar.querySelector<HTMLSelectElement>('[data-vehicle-select]')!;const postcode=bar.querySelector<HTMLInputElement>('[data-postcode-input]')!;const radius=bar.querySelector<HTMLSelectElement>('[data-radius-select]')!;const applyBtn=bar.querySelector<HTMLButtonElement>('[data-apply-distance]')!;const clearBtn=bar.querySelector<HTMLButtonElement>('[data-clear-distance]')!;const result=bar.querySelector<HTMLElement>('[data-filter-result]')!;const message=bar.querySelector<HTMLElement>('[data-distance-message]')!;
 const applyFilters=()=>{let visible=0;section.querySelectorAll<HTMLElement>('.jobOpportunity').forEach(card=>{const vehicleOk=vehicle.value==='ALL'||card.dataset.vehicleType===vehicle.value;const distanceOk=!bar?.dataset.distanceActive||((Number(card.dataset.distanceMiles)<=Number(radius.value))&&card.dataset.distanceMiles!=='');const show=vehicleOk&&distanceOk;card.style.display=show?'':'none';if(show)visible++});result.textContent=`${visible} job${visible===1?'':'s'} shown`};
 if(!vehicle.dataset.bound){vehicle.dataset.bound='true';vehicle.addEventListener('change',applyFilters)}
 if(!applyBtn.dataset.bound){applyBtn.dataset.bound='true';applyBtn.addEventListener('click',async()=>{const value=postcode.value.trim();if(!value){message.textContent='Enter your current postcode first.';return}applyBtn.disabled=true;applyBtn.textContent='Checking distance…';message.textContent='';try{const r=await fetch('/api/jobs/radius',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({postcode:value,jobs:jobs.map(j=>({id:j.id,collection:j.collection}))})});const d=await r.json().catch(()=>null);if(!r.ok)throw new Error(d?.error||'Unable to calculate distances');const map=new Map((d.distances||[]).map((x:any)=>[String(x.id),x.miles]));section.querySelectorAll<HTMLElement>('.jobOpportunity').forEach(card=>{const miles=map.get(card.dataset.jobId||'');card.dataset.distanceMiles=typeof miles==='number'?String(miles):''});bar!.dataset.distanceActive='true';clearBtn.style.display='inline-flex';message.textContent=`Showing collection points within ${radius.value} miles of ${d.origin||value}.`;applyFilters()}catch(e:any){message.textContent=e?.message||'Unable to calculate job distances.'}finally{applyBtn.disabled=false;applyBtn.textContent='Apply distance'}})}
 if(!clearBtn.dataset.bound){clearBtn.dataset.bound='true';clearBtn.addEventListener('click',()=>{delete bar!.dataset.distanceActive;section.querySelectorAll<HTMLElement>('.jobOpportunity').forEach(card=>delete card.dataset.distanceMiles);clearBtn.style.display='none';message.textContent='Distance filter cleared.';applyFilters()})}
 if(!radius.dataset.bound){radius.dataset.bound='true';radius.addEventListener('change',()=>{if(bar?.dataset.distanceActive){message.textContent=`Showing collection points within ${radius.value} miles of ${postcode.value.trim()}.`;applyFilters()}})}
 applyFilters()}
export default function TransporterCardExpander(){const pathname=usePathname();useEffect(()=>{if(pathname!=='/transporter')return;let jobs:any[]=[];let cancelled=false;let scheduled=false;const scan=()=>{scheduled=false;document.querySelectorAll<HTMLElement>('main.dashboardShell .transporterBooking:not([data-transporter-expandable="true"])').forEach(card=>enhance(card));document.querySelectorAll<HTMLElement>('main.dashboardShell .jobOpportunity:not([data-transporter-expandable="true"])').forEach((card,i)=>enhance(card,jobs[i]));ensureJobFilters(jobs)};const scheduleScan=()=>{if(scheduled||cancelled)return;scheduled=true;window.requestAnimationFrame(scan)};const load=async()=>{try{const r=await fetch('/api/jobs',{cache:'no-store'});if(r.ok)jobs=await r.json()}catch{}if(cancelled)return;scheduleScan()};load();scheduleScan();const observer=new MutationObserver(scheduleScan);observer.observe(document.body,{childList:true,subtree:true});return()=>{cancelled=true;observer.disconnect();document.querySelector('[data-job-filters]')?.remove();document.querySelectorAll<HTMLElement>('#available-jobs .jobOpportunity').forEach(card=>card.style.display='')}},[pathname]);return null}
