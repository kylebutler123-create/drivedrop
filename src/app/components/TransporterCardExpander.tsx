'use client';
import {useEffect} from 'react';
import {usePathname} from 'next/navigation';
function text(el:Element|null){return (el?.textContent||'').replace(/\s+/g,' ').trim()}
function escapeHtml(value:any){return String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]||c))}
function detailValue(card:HTMLElement,label:string){const rows=Array.from(card.querySelectorAll<HTMLElement>('.activeVehicleDetailsGrid>span'));const row=rows.find(x=>text(x.querySelector('small')).toLowerCase()===label.toLowerCase());return text(row?.querySelector('b')||null)}
function deliveryFacts(card:HTMLElement){const vehicleType=detailValue(card,'Vehicle type')||'Not specified';const registration=detailValue(card,'Registration')||'Not provided';const running=detailValue(card,'Running condition')||'Not specified';const date=text(card.querySelector('.agreedCollectionDate strong'))||detailValue(card,'Collection date')||'Not specified';const proceeds=text(card.querySelector('.paymentMini strong'))||'—';return `<span class="transporterCollapsedFacts transporterDeliveryFacts"><span><small>Vehicle</small><b>${escapeHtml(vehicleType)}</b></span><span><small>Reg</small><b>${escapeHtml(registration)}</b></span><span><small>Date</small><b>${escapeHtml(date)}</b></span><span><small>Condition</small><b>${escapeHtml(running)}</b></span><span class="wide"><small>Proceeds</small><b>${escapeHtml(proceeds)}</b></span></span>`}
function enhance(card:HTMLElement,job?:any){if(card.dataset.transporterExpandable==='true')return;card.dataset.transporterExpandable='true';card.classList.add('transporterExpandableCard','isCollapsed');const btn=document.createElement('button');btn.type='button';btn.className='transporterCardToggle';btn.setAttribute('aria-expanded','false');const status=text(card.querySelector('.statusPill'));const title=text(card.querySelector('.bookingTop h2'))||'DriveDrop card';const partner=text(card.querySelector('.bookingPartner'));const route=card.querySelector('.routeVisual');const routeStops=route?Array.from(route.querySelectorAll('b')).map(x=>text(x)).filter(Boolean):[];const proceeds=text(card.querySelector('.paymentMini strong'));const vehicleState=text(card.querySelector('.vehicleState b'));const kind=card.classList.contains('jobOpportunity')?'job':'delivery';const meta=routeStops.length>=2?`${routeStops[0]} → ${routeStops[1]}`:partner;let stat=kind==='job'?(vehicleState||'Open for quotes'):(proceeds?`Proceeds · ${proceeds}`:partner||'View delivery');let quickFacts=kind==='delivery'?deliveryFacts(card):'';if(kind==='job'&&job){const quoted=(job.quotes||[]).length>0;stat=quoted?'Already quoted':'Not yet quoted';quickFacts=`<span class="transporterCollapsedFacts"><span><small>Vehicle</small><b>${escapeHtml(job.vehicleType||'Not specified')}</b></span><span><small>Reg</small><b>${escapeHtml(job.registration||'Not provided')}</b></span><span><small>Date</small><b>${job.collectionDate?new Date(job.collectionDate).toLocaleDateString('en-GB',{day:'numeric',month:'short'}):'Not provided'}</b></span><span><small>Condition</small><b>${job.running?'Runs & drives':'Non-running'}</b></span></span>`;const panel=document.createElement('div');panel.className='transporterJobRequestDetails';panel.innerHTML=`<div><span>Vehicle type</span><b>${escapeHtml(job.vehicleType||'Not specified')}</b></div><div><span>Make & model</span><b>${escapeHtml(`${job.vehicleMake||''} ${job.vehicleModel||''}`.trim())}</b></div><div><span>Registration</span><b>${escapeHtml(job.registration||'Not provided')}</b></div><div><span>Running condition</span><b>${job.running?'Runs & drives':'Non-running'}</b></div><div><span>Requested collection</span><b>${job.collectionDate?new Date(job.collectionDate).toLocaleDateString('en-GB'):'Not provided'}</b></div><div><span>Quote status</span><b>${quoted?'You have quoted':'Not yet quoted'}</b></div><div class="wide"><span>Collection</span><b>${escapeHtml(job.collection||'—')}</b></div><div class="wide"><span>Delivery</span><b>${escapeHtml(job.delivery||'—')}</b></div>`;const bookingTop=card.querySelector('.bookingTop');if(bookingTop?.nextSibling)card.insertBefore(panel,bookingTop.nextSibling);else card.appendChild(panel)}btn.innerHTML=`<span class="transporterCardSummaryMain"><span class="transporterCardSummaryStatus">${escapeHtml(status||(kind==='job'?'Open for quotes':'Delivery'))}</span><strong>${escapeHtml(title)}</strong><small>${escapeHtml(meta)}</small>${quickFacts}</span><span class="transporterCardSummarySide"><b>${escapeHtml(stat)}</b><span class="transporterCardChevron">+</span></span>`;const toggle=()=>{const collapsed=card.classList.toggle('isCollapsed');btn.setAttribute('aria-expanded',collapsed?'false':'true');const c=btn.querySelector('.transporterCardChevron');if(c)c.textContent=collapsed?'+':'−'};btn.addEventListener('click',toggle);card.insertBefore(btn,card.firstChild)}
export default function TransporterCardExpander(){const pathname=usePathname();useEffect(()=>{if(pathname!=='/transporter')return;let jobs:any[]=[];let cancelled=false;let scheduled=false;const scan=()=>{scheduled=false;document.querySelectorAll<HTMLElement>('main.dashboardShell .transporterBooking:not([data-transporter-expandable="true"])').forEach(card=>enhance(card));document.querySelectorAll<HTMLElement>('main.dashboardShell .jobOpportunity:not([data-transporter-expandable="true"])').forEach((card,i)=>enhance(card,jobs[i]))};const scheduleScan=()=>{if(scheduled||cancelled)return;scheduled=true;window.requestAnimationFrame(scan)};const load=async()=>{try{const r=await fetch('/api/jobs',{cache:'no-store'});if(r.ok)jobs=await r.json()}catch{}if(cancelled)return;scheduleScan()};load();scheduleScan();const observer=new MutationObserver(scheduleScan);observer.observe(document.body,{childList:true,subtree:true});return()=>{cancelled=true;observer.disconnect()}},[pathname]);return null}
